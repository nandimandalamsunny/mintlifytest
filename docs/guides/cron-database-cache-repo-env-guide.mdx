---
title: "Cron + Database + Cache: Repo Preference & Environment Settings"
---

This guide explains how to configure a Cron service with both a Database and a Cache in Revolte, covering Repo Preference and Environment Settings. Visual references are provided for each step.

## Step 1: Choose a Preset

Select the **Cron + Database + Cache** preset to automate scheduled tasks with both database and cache support.

![Choose Cron + Database + Cache Preset](../../images/preset-11.png)

- **Cron + Database + Cache**: Schedule tasks with database and cache support (e.g., periodic jobs with persistent data and fast access).

## Step 2: Repo Preference

Configure repository interaction for your service:

- **Application and branch setting**: Choose the branch and framework for your scheduled job.
- **Setup Agent Workflow**: Enable to automate developer workflows.
- **Set up pre build Workflow**: Enable code review and summarization before builds.
- **Pipeline**: Configure code quality and test suite steps.
- **Setup Build & deployment workflow**: Enable to automate build and deployment for your service.

## Step 3: Environment Settings

After repo preferences, configure environment-specific settings for your service.

### 3.1 Environment Settings Overview

![Environment Settings Overview](../../images/environment_settings/envsettings-11.png)

In the Environment Settings panel, select the **Cron**, **Database**, and **Cache** tabs to configure scheduled job, database, and cache details for each environment (QA, Staging, Production, Preview).

#### Fields in Cron Tab:
- **Service Name**: Unique name for the cron job (e.g., data-cache-cron)
- **Entry Point**: Main file to launch (e.g., index.js)
- **Schedule**: Cron expression defining when the job runs (e.g., `0 0 * * *` for daily at midnight)
- **Specifications**: Define CPU, memory, and storage requirements
- **CPU & Memory**: Set resource limits for the job
- **Storage Value**: Specify disk storage allocation
- **Properties**:
  - **Job Timeout**: Maximum allowed runtime for the job
  - **Retries**: Number of retry attempts on failure
- **Pre Script**: List of scripts to run before the job starts (empty by default)
- **Variables**: Set environment variables (e.g., CACHE_URL, DB_CONNECTION_STRING)

#### Fields in Database Tab:
- **Database Type**: e.g., PostgreSQL, MySQL
- **Database Name**: Name for your database
- **User/Password**: Credentials for database access
- **Connection String**: Auto-generated or custom
- **Storage**: Disk allocation for the database
- **Backup & Restore**: Options for data safety
- **Variables**: Environment variables for database connection

#### Fields in Cache Tab:
- **Cache Type**: e.g., Redis, Memcached
- **Cache Name**: Name for your cache instance
- **Max Memory**: Maximum memory allocation for the cache
- **Eviction Policy**: How cache evicts entries (e.g., LRU, LFU)
- **Variables**: Environment variables for cache connection

### 3.2 Example: Staging Environment

![Staging Cron + Database + Cache Example](../../images/environment_settings/envsettings-11-1.png)

- **Service Name**: data-cache-cron
- **Entry Point**: index.js
- **Schedule**: `0 0 * * *` (runs daily at midnight)
- **Specifications**: Set as needed for your workload
- **CPU & Memory**: Configure based on expected usage
- **Storage Value**: Allocate disk space
- **Properties**:
  - **Job Timeout**: 30m
  - **Retries**: 3
- **Pre Script**: (empty)
- **Variables**: CACHE_URL, DB_CONNECTION_STRING

![Staging Database Example](../../images/environment_settings/envsettings-11-2.png)

- **Database Type**: PostgreSQL
- **Database Name**: revolte_db
- **User/Password**: Set credentials for access
- **Connection String**: Provided for application use
- **Storage**: Allocate disk space for the database
- **Backup & Restore**: Configure as needed
- **Variables**: Set DB connection variables for your app

![Staging Cache Example](../../images/environment_settings/envsettings-11-3.png)

- **Cache Type**: Redis
- **Cache Name**: revolte-cache
- **Max Memory**: 512MB
- **Eviction Policy**: LRU
- **Variables**: Set cache connection variables for your app

#### How to Configure Each Field
1. Fill in all required fields for Cron, Database, and Cache tabs as described above.
2. Repeat for each environment tab (QA, Staging, Production, Preview) as needed.

### Commit Details

At the bottom of the configuration panel, you will find **Commit Details**:
- **File Name**: The YAML file to be committed (e.g., revolte.yml)
- **Commit Message**: Enter a descriptive message for your changes.
- **Choose Branch**: Select the branch to commit the configuration to.
- **Commit**: Save and apply your configuration.
- **Skip & deploy**: Skip commit and proceed to deployment (not recommended for production).

> **Tip:** Always review your configuration and commit with a clear message to maintain traceability.

For more details, refer to the [Guide Overview](./overview.mdx).
