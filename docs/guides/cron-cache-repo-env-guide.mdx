---
title: "Cron + Cache: Repo Preference & Environment Settings"
description: "Configure repository preferences and environment settings for a cron service with cache."
---

This guide explains how to configure a Cron service with an attached Cache in Revolte, covering both Repo Preference and Environment Settings. Visual references are provided for each step.

## Prerequisites

Before you configure this preset, make sure you have:

- A Revolte workspace with an application created
- Repository access for the target application
- Permission to edit YAML configuration and commit changes
- Environment-specific values for QA, staging, production, and preview

## Step 1: Choose a Preset

Select the **Cron + Cache** preset to automate scheduled tasks with managed caching.

![Choose Cron + Cache Preset](/images/guides/repo_preference/preset-9.png)

- **Cron + Cache**: Schedule tasks with cache support (e.g., periodic jobs that use Redis or Memcached).
- Other options include Web app, Web service, and combinations with databases or storage.

## Step 2: Repo Preference

Configure repository interaction for your cron service with cache:

- **Application and branch setting**: Choose the branch and framework for your scheduled job.
- **Agent workflow**: Enable to automate developer workflows.
- **Pre-build workflow**: Enable code review and summarization before builds.
- **Pipeline**: Configure code quality and test suite steps.
- **Build and deployment workflow**: Enable to automate build and deployment for your service.

## Step 3: Environment Settings

After repo preferences, configure environment-specific settings for your cron service with cache.

### 3.1 Environment Settings Overview

![Environment Settings Overview](/images/guides/environment_settings/envsettings-9.png)

In the Environment Settings panel, select the **Cron** and **Cache** tabs to configure scheduled job and cache details for each environment (QA, Staging, Production, Preview).

#### Fields in Cron Tab:
- **Service Name**: Unique name for the cron job (e.g., cache-sync-cron)
- **Entry Point**: Main file to launch (e.g., index.js)
- **Schedule**: Cron expression defining when the job runs (e.g., `0 0 * * *` for daily at midnight)
- **Specifications**: Define CPU, memory, and storage requirements
- **CPU & Memory**: Set resource limits for the job
- **Storage Value**: Specify disk storage allocation
- **Properties**:
  - **Job Timeout**: Maximum allowed runtime for the job
  - **Retries**: Number of retry attempts on failure
- **Pre Script**: List of scripts to run before the job starts (empty by default)
- **Variables**: Set environment variables (e.g., CACHE_URL)

#### Fields in Cache Tab:
- **Cache Type**: e.g., Redis, Memcached
- **Cache Name**: Name for your cache instance
- **Max Memory**: Maximum memory allocation for the cache
- **Eviction Policy**: How cache evicts entries (e.g., LRU, LFU)
- **Variables**: Environment variables for cache connection

### 3.2 Example: Staging Environment

![Staging Cron + Cache Example](/images/guides/environment_settings/envsettings-9-1.png)

- **Service Name**: cache-sync-cron
- **Entry Point**: index.js
- **Schedule**: `0 0 * * *` (runs daily at midnight)
- **Specifications**: Set as needed for your workload
- **CPU & Memory**: Configure based on expected usage
- **Storage Value**: Allocate disk space
- **Properties**:
  - **Job Timeout**: 30m
  - **Retries**: 3
- **Pre Script**: (empty)
- **Variables**: CACHE_URL

![Staging Cache Example](/images/guides/environment_settings/envsettings-9-2.png)

- **Cache Type**: Redis
- **Cache Name**: revolte-cache
- **Max Memory**: 512MB
- **Eviction Policy**: LRU
- **Variables**: Set cache connection variables for your app

#### How to Configure Each Field
1. Fill in all required fields for Cron and Cache tabs as described above.
2. Repeat for each environment tab (QA, Staging, Production, Preview) as needed.

### Commit Details

At the bottom of the configuration panel, you will find **Commit Details**:
- **File Name**: The YAML file to be committed (e.g., revolte.yml)
- **Commit Message**: Enter a descriptive message for your changes.
- **Choose Branch**: Select the branch to commit the configuration to.
- **Commit**: Save and apply your configuration.
- **Skip & deploy**: Skip commit and proceed to deployment (not recommended for production).

> **Tip:** Always review your configuration and commit with a clear message to maintain traceability.

For more details, refer to the [configuration guide overview](/docs/guides/overview).
