---
title: "Web Service with Storage Bucket & Cache"
description: "Configure repository preferences and environment settings for a web service with storage and cache."
---

This guide explains how to configure a Web service with both a Storage Bucket and a Cache in Revolte, covering Repo Preference and Environment Settings. Visual references are provided for each step.


<Steps>
  <Step title="Choose a Preset">
    ![Choose Webservice + StorageBucket + Cache Preset](/images/guides/repo_preference/preset-12.png)

    The preset chooser shows a grid of service options. The **Webservice + Storage Bucket + Cache** tile is highlighted. Click **Webservice + Storage Bucket + Cache** to continue.
  </Step>

  <Step title="Repo Preference">
    The Repo Preference tab is identical across all service presets. See [Repository Preference](/docs/guides/overview#repository-preference) for a full walkthrough of each setting.
  </Step>

  <Step title="Webservice — Environment Settings">
    ![Webservice tab — QA environment](/images/guides/environment_settings/envsettings-2.png)

    The **QA** environment tab is selected. The left sidebar shows **Webservice**, **Storage Bucket**, **Cache**, and **Variables** under Build & deployment workflow. The center panel shows the Webservice form:

    - **Service Name** — Enter a unique identifier for this service (e.g., `web_service`).
    - **Entry Point** — The main file that starts the service (e.g., `index.js`).
    - **Specifications**
      - **CPU & Memory** — Choose a compute tier for this service (e.g., `2 CPU and 1 GB Memory`).
      - **Storage value** — Disk space in GB allocated to the service (e.g., `30`).
    - **Properties**
      - **Health EndPoint** — The URL path Revolte checks to confirm the service is running (e.g., `/health`).
      - **Port** — The port your service listens on (e.g., `8080`).
    - **Pre Script** — Commands to run before the service starts. Click **+ Add** to add a script.
    - **Load Balancer Mode** — How incoming traffic is routed. Choose **Dedicated** for an isolated load balancer or **Shared** to share one across services.
    - **Insights** — Select system metrics to monitor:
      - **Cpu** — Options: idle, user, system.
      - **Swap** — Options: swap usage.
      - **Memory** — Options: total memory used, free memory.
      - **Disk** — Options: disk used, disk total, disk free.
  </Step>

  <Step title="Storage Bucket — Environment Settings">
    ![Storage Bucket tab — QA environment](/images/guides/environment_settings/envsettings-12-1.png)

    The **QA** environment tab is selected with the **Storage Bucket** tab active. The left sidebar shows **Web service**, **Storage Bucket**, **Cache**, and **Variables**. The center panel shows the Storage Bucket form:

    - **Service Name** — Enter a unique identifier for this storage bucket service (e.g., `my_storage`).
    - **Configurations**
      - **Bucket Name** — Enter a globally unique name for the bucket (e.g., `my-app-assets`).
      - **Bucket Versioning** — Toggle on to keep a history of object versions for rollback or audit.
      - **Enable Public Access** — Toggle on to make bucket contents publicly accessible via URL.
    - **Cors Rules** — Define allowed origins, methods, and headers for cross-origin requests. Click **+ Add** to add a rule.
  </Step>

  <Step title="Cache — Environment Settings">
    ![Cache tab — QA environment](/images/guides/environment_settings/envsettings-12-2.png)

    The **QA** environment tab is selected with the **Cache** tab active. The left sidebar shows **Web service**, **Storage Bucket**, **Cache**, and **Variables**. The center panel shows the Cache form:

    - **Service Name** — Enter a unique identifier for this cache instance (e.g., `my_cache`).
    - **Specifications**
      - **CPU & Memory** — Choose a resource tier that matches your caching needs (e.g., `2 CPU and 1 GB Memory`).
  </Step>

  <Step title="Variables & Secrets">
    ![Variables & Secrets tab — QA environment](/images/guides/environment_settings/envsettings-12-3.png)

    The **QA** environment tab is selected with the **Secrets** sub-tab active. The left sidebar shows **Web service**, **Storage Bucket**, **Cache**, and **Variables**. The center panel shows:

    - **Secret Engine** — **Revolte secrets** is the built-in provider for storing encrypted key-value pairs.
    - **Add Secrets** button — Click to add a secret. Enter a key (e.g., `DATABASE_URL`) and its value (e.g., a connection string or API key).
    - Secrets appear as rows in the table once added. The table is empty until the first secret is created.
  </Step>

  <Step title="Commit">
    At the bottom of the panel, fill in the **Commit Details**:

    - **File Name** — The configuration file to create or update in your repository (e.g., `revolte.yml`).
    - **Commit Message** — A short description of what you configured (e.g., `Add webservice with storage bucket and cache`).
    - **Choose Branch** — The branch where the configuration file will be committed.

    Click **Commit** to save the configuration to your repository, or **Skip & deploy** to deploy immediately without committing.
  </Step>
</Steps>

> **Tip:** Always commit with a clear message before deploying to keep a traceable configuration history.
